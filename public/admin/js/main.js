layui.use('element', function(){
  	var element = layui.element;
  	var $ = layui.$;
  // 	$("dl dd a").on("click",function(){  
  // 		var othis = $(this);
  // 		// alert(othis.attr("value"));
  // 		var li = $("li[lay-id="+$(this).text()+"]").length;
		// if(li<1){
		// 	element.tabAdd('tab',{
	 //        	title: $(this).text()
		// 	  	,content: '<iframe width="100%" height="600"  frameborder="0"  src="./page/'+$(this).attr("value")+'"></iframe>',
		// 	  	id:$(this).text(),
	 //        });
		// }		
  //       element.tabChange('tab', $(this).text());
 	// });  
});